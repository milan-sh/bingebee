import {UserRoundCheck} from "lucide-react"
import Button from "./Button"

const SubscribeButton = () => {
  return (
    <Button className="flex items-center gap-2 text-[10px]"><UserRoundCheck /> Subscribe</Button>
  )
}

export default SubscribeButton
